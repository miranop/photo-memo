# photo-memo API 仕様書

写真付きメモを管理するバックエンドAPI。このドキュメントは、このAPI上に好きなUIを構築するAI(あるいは人間)向けのリファレンス。

## 0. まず把握しておくべき注意点

UIを作る際に踏み外しやすい点を先に挙げる。

1. **エラーレスポンスはJSONではなくプレーンテキスト。** `Content-Type: text/plain`、ボディはメッセージ文字列そのまま(例: `memo not find`)。`error.message` のようなJSONパースは失敗する。判定は必ずHTTPステータスコードで行うこと。
2. **一覧系エンドポイントは、0件のとき配列 `[]` ではなく JSON の `null` を返す。**(GoのnilスライスがそのままJSON化されるため。)フロント側は `null` を空配列として扱う必要がある。
3. **JSONのフィールド名はすべて先頭大文字(PascalCase)。**(`ID`, `Title`, `CreatedAt` など。Goの構造体名がそのまま出力されており、`camelCase`変換は入っていない。)
4. **`Photo.FilePath` はサーバー内部のファイルパスであり、ブラウザから直接アクセスできるURLではない。** 画像を表示するには `GET /photos/{id}` を叩く必要がある(詳細は3章)。
5. **CORSヘッダーは一切設定されていない。** UIをこのサーバーと別オリジン(別ポートの開発サーバーなど)で動かす場合、ブラウザからのfetchはCORSエラーで失敗する。同一オリジンで配信するか、サーバー側にCORS対応を追加する必要がある。
6. **認証機構はまだ無い。** 全メモは `UserID: 1` に固定されている(`internal/memo/store.go` に `TODO` コメントあり)。ログイン/ユーザー切り替えのUIを作っても意味を持たない。

## 1. 基本情報

- ベースURL: `http://localhost:8080`(ポートはハードコード、環境変数等での変更は未対応)
- リクエストボディ: JSON(`Content-Type: application/json`) ※画像アップロードのみ `multipart/form-data`
- レスポンスボディ: 成功時はJSON(`Content-Type: application/json`)、エラー時はプレーンテキスト
- 文字コード: UTF-8
- 日時形式: RFC3339Nano、常にUTC(例: `"2026-08-17T13:39:35.487201554Z"`)

## 2. データモデル

### Memo

| フィールド | 型 | 説明 |
|---|---|---|
| `ID` | number | メモID |
| `UserID` | number | 常に `1`(固定値、認証未実装のため) |
| `Title` | string | タイトル。必須(空文字は許容されるがカラム自体はNOT NULL) |
| `Body` | string | 本文。空文字可 |
| `CreatedAt` | string(RFC3339Nano) | 作成日時 |
| `UpdatedAt` | string(RFC3339Nano) | 更新日時。作成直後は `CreatedAt` と同値 |

```json
{
  "ID": 4,
  "UserID": 1,
  "Title": "買い物メモ",
  "Body": "牛乳と卵",
  "CreatedAt": "2026-08-17T13:39:35.487201554Z",
  "UpdatedAt": "2026-08-17T13:39:35.487201554Z"
}
```

### Photo

| フィールド | 型 | 説明 |
|---|---|---|
| `ID` | number | 写真ID |
| `MemoID` | number | 紐づくメモのID |
| `FilePath` | string | サーバー内部の保存パス(例: `photos/44dd80b5....png`)。**UIから直接使わないこと**、画像取得は `GET /photos/{id}` を使う |
| `CreatedAt` | string(RFC3339Nano) | アップロード日時 |

```json
{
  "ID": 2,
  "MemoID": 3,
  "FilePath": "photos/44dd80b57efb712a598407fa4ea1171c.png",
  "CreatedAt": "2026-08-17T13:34:14.240445794Z"
}
```

## 3. エンドポイント一覧

| メソッド | パス | 概要 |
|---|---|---|
| POST | `/memos` | メモ作成 |
| GET | `/memos` | メモ一覧取得 |
| GET | `/memos/{id}` | メモ詳細取得 |
| PATCH | `/memos/{id}` | メモ部分更新 |
| DELETE | `/memos/{id}` | メモ削除(紐づく写真も連動して削除される) |
| POST | `/memos/{id}/photos` | 写真アップロード |
| GET | `/memos/{id}/photos` | メモに紐づく写真一覧取得 |
| GET | `/photos/{id}` | 写真の実データ(画像バイナリ)を取得 |
| DELETE | `/photos/{id}` | 写真削除 |

---

### POST /memos — メモ作成

リクエスト:
```json
{ "Title": "買い物メモ", "Body": "牛乳と卵" }
```

レスポンス: `201 Created`、作成された `Memo` オブジェクト。

エラー:
| コード | 条件 |
|---|---|
| 400 | JSONのパースに失敗 |
| 500 | DBエラー |

---

### GET /memos — メモ一覧取得

レスポンス: `200 OK`、`Memo` の配列。0件なら `null`。

---

### GET /memos/{id} — メモ詳細取得

レスポンス: `200 OK`、`Memo` オブジェクト。

エラー:
| コード | 条件 |
|---|---|
| 400 | `{id}` が数値でない |
| 404 | 該当メモが存在しない |
| 500 | DBエラー |

---

### PATCH /memos/{id} — メモ部分更新

リクエストは更新したいフィールドのみを含める(省略したフィールドは変更されない)。

```json
{ "Title": "新しいタイトル" }
```
```json
{ "Body": "本文だけ変えたい" }
```

レスポンス: `200 OK`、更新後の `Memo` オブジェクト全体。

エラー:
| コード | 条件 |
|---|---|
| 400 | `{id}` が数値でない / JSONパース失敗 |
| 404 | 該当メモが存在しない |
| 500 | DBエラー |

---

### DELETE /memos/{id} — メモ削除

紐づく `Photo` レコードもDB上はカスケード削除される(**ただし保存済みの画像ファイル自体はディスクから消えない**。物理ファイルの掃除は未実装 — 既知の制限として4章参照)。

レスポンス: `204 No Content`(ボディなし)。

エラー:
| コード | 条件 |
|---|---|
| 400 | `{id}` が数値でない |
| 404 | 該当メモが存在しない |
| 500 | DBエラー |

---

### POST /memos/{id}/photos — 写真アップロード

`multipart/form-data` でフィールド名 `photo` に画像ファイルを1つ入れて送る。

```bash
curl -X POST http://localhost:8080/memos/3/photos \
  -F "photo=@image.png;type=image/png"
```

制約:
- 許可される拡張子: `.jpg` `.jpeg` `.png` `.gif` `.webp`(拡張子だけでなく、ファイルの中身も検査してMIMEタイプを確認している。拡張子を偽装したファイルは拒否される)
- 最大サイズ: 10MB
- アップロード先メモ(`{id}`)が存在しない場合は拒否される

レスポンス: `201 Created`、作成された `Photo` オブジェクト。

エラー:
| コード | 条件 |
|---|---|
| 400 | `{id}` が数値でない / `photo` フィールドが無い、または不正なファイル / 拡張子・中身が非対応形式 |
| 404 | 紐づけ先のメモが存在しない |
| 413 | ファイルサイズが10MB超過 |
| 500 | ファイル保存・DBエラー |

---

### GET /memos/{id}/photos — 写真一覧取得

レスポンス: `200 OK`、`Photo` の配列。0件なら `null`。

エラー:
| コード | 条件 |
|---|---|
| 400 | `{id}` が数値でない |
| 404 | 該当メモが存在しない |
| 500 | DBエラー |

---

### GET /photos/{id} — 写真データ取得(画像表示用)

写真そのもの(画像バイナリ)を返す。`Content-Type` はファイルの拡張子から自動判定される(`image/png` など)。`<img>` タグの `src` に直接指定できる。

```html
<img src="http://localhost:8080/photos/2" />
```

レスポンス: `200 OK`、画像バイナリ。

エラー:
| コード | 条件 |
|---|---|
| 400 | `{id}` が数値でない |
| 404 | 該当写真が存在しない |
| 500 | DBエラー |

---

### DELETE /photos/{id} — 写真削除

DBレコードとディスク上のファイルの両方を削除する。

レスポンス: `204 No Content`(ボディなし)。

エラー:
| コード | 条件 |
|---|---|
| 400 | `{id}` が数値でない |
| 404 | 該当写真が存在しない |
| 500 | DBエラー |

## 4. 既知の制限(UI設計時に踏まえておくこと)

- **認証なし。** 全操作がユーザー1人分として扱われる。ログイン画面・ユーザー切り替えUIは不要。
- **CORS未設定。** サーバーと別オリジンでUIを動かす場合、fetch/XHRがブロックされる。
- **ページネーションなし。** `GET /memos` は常に全件返す。メモが大量にある想定のUIは今のところ意味がない。
- **メモ削除時に画像ファイルが物理削除されない。** DBレコードは消えるが `photos/` ディレクトリにファイルが残り続ける(孤立ファイル)。
- **写真の並び替え・キャプション・複数枚の順序管理は無い。** `Photo` は作成日時とメモIDしか持たない。
- **HTTPS/認証ヘッダーなし。** ローカル開発を想定した最小構成。
